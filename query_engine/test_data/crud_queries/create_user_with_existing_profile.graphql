mutation CreateUserWithProfile($userWithExistingProfile: UserCreateInput!) {
  createUser(data: $userWithExistingProfile) {
    id
    email
    nickNames
    profile {
      id
      bio
    }
  }
}
